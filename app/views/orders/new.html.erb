<%= form_for @order do |f| %>
  <%= f.hidden_field :date, :value => Time.now %>
  <%= f.fields_for :order_items do |f| %>
    <%= f.collection_select :item_id, Item.all, :id, :item_type %>
    <%= f.label "Quantity: "%>
    <%= f.text_field :quantity %><br>
  <% end %>
  <%= f.submit %>
<% end %>
